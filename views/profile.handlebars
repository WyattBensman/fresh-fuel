    <div class="container">
        <div class="row mt-5">
            <div class="col-3 d-flex flex-column align-items-center mt-3">
                <h2>Your Recipes</h2>
                <div class="card px-3 flex" style="width: 13rem; height: 72px;">
                    <div class="card-body">
                        <div class="row">
                            <a href="/create-dish" class="btn btn-success">Create Dish!</a>
                        </div>
                    </div>
                </div>
            </div>
            <!-- CREATED CARD -->
            <div class="col-9 d-flex flex-row flex-wrap yourRecipesContainer">
                {{#each userRecipes}}
                {{> recipeProfileCard}}
                {{/each}}
            </div>
        </div>
    </div>
